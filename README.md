# Gophkeeper

Клиент-серверное приложение для управления личными данными. Имеет TUI, которое реализовано на библиотеке bubbletea. Тестирование проводится через Docker.

## Быстрый старт

```bash
docker-compose up --build -d
docker-compose run --rm client
```
